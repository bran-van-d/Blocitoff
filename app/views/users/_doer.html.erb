<h1> Doers </h1>

  <div class="row">
    <div class="col-md-8">
      <% @users.each do |user| %>
        <div class="media">
          <div class="media-body">
            <h4 class="media-heading">
              <%= link_to user.first_name, user %>
            </h4>
          </div>
        </div>
      <% end %>
    </div>
